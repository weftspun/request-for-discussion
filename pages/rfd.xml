<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Taskweft — Manuals</title>
<link>https://weftspun.github.io/request-for-discussion/pages/rfd.html</link>
<atom:link href="https://weftspun.github.io/request-for-discussion/pages/rfd.xml" rel="self" type="application/rss+xml"/>
<description>Request-for-Discussion documents, the serial register, and the logbook.</description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Sat, 05 Sep 2026 22:18:44 GMT</lastBuildDate>
<item>
  <title>RFD 2230: ggml model adapters as sandboxed GDScript over one native module</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2230-ggml-adapters-in-godot-sandbox/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L2 (coordination — collapses N per-model C++ modules into 1 shared native + N GDScript adapters) <code>entities-godot-sandbox</code>; model-specific adapter code (tokenizer, prompt templates, LoRA scaling, sampler config, per-model quirks) moves out of per-project C++ into GDScript loaded at runtime with sandbox permissions bricks-cpp / kimodo / skin-tokens-cpp adapter code, RFD 2212 (motion-bricks-as-native-module rescopes), RFD 2229 (this is one of its named consolidations landing)</p>
<section id="operator-directive" class="level2">
<h2 class="anchored" data-anchor-id="operator-directive">Operator directive</h2>
<p>Operator, 2026-09-05, verbatim: <em>“Are you able to move the ggml model adapters and process from c++ to godot-sandbox gdscript if we add ggml to entities-godot?”</em>.</p>
<p><strong>Answer:</strong> yes, cleanly, with a native/adapter split. The precondition — adding ggml as a shared module under <code>entities-godot-sandbox</code> — is exactly the shape RFD 2229’s interchangeable-parts policy asks for: one ggml surface every consumer links, not N per-project C++ modules.</p>
</section>
<section id="the-layering" class="level2">
<h2 class="anchored" data-anchor-id="the-layering">The layering</h2>
<p><strong>Layer 1 — native, one module.</strong> <code>entities-godot-sandbox/modules/ggml/</code> hosts <code>2-contract/ggml/</code> (RFD 2188 canonical source) plus ggml’s Vulkan backend (MoltenVK on macOS). Exposes a small GDExtension surface, GDScript-callable:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode gdscript code-with-copy"><code class="sourceCode gdscript"><span id="cb1-1"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">var</span> model :<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Ggml.<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">load_model</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"res://models/motion-bricks.gguf"</span>)</span>
<span id="cb1-2"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">var</span> lora :<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Ggml.<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">load_lora</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"res://models/motion-bricks-walk.lora"</span>)</span>
<span id="cb1-3">model.<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">apply_lora</span>(lora)</span>
<span id="cb1-4"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">var</span> tokens :<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> Ggml.<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">tokenize</span>(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"walk forward 60 frames"</span>)</span>
<span id="cb1-5"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">var</span> out :<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> model.<span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">run_inference</span>(tokens, {<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"max_new_tokens"</span>: <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">60</span>})</span></code></pre></div></div>
<p>The native module handles: model file mmap, KV cache, backend dispatch (CPU / Metal / Vulkan / WebGPU), sampler primitives, tensor arithmetic. Nothing about a specific model’s chat template or LoRA layout lives here.</p>
<p><strong>Layer 2 — GDScript, per-model adapters.</strong> One <code>.gd</code> file per model. Each adapter is a Resource subclass extending <code>GgmlAdapter</code>:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode gdscript code-with-copy"><code class="sourceCode gdscript"><span id="cb2-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># res://adapters/motion_bricks.gd</span></span>
<span id="cb2-2"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">class_name</span> MotionBricksAdapter <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">extends</span> GgmlAdapter</span>
<span id="cb2-3"></span>
<span id="cb2-4"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">const</span> MODEL_PATH :<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"res://models/motion-bricks.gguf"</span></span>
<span id="cb2-5"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">const</span> TOKENIZER :<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"sentencepiece-motion"</span></span>
<span id="cb2-6"></span>
<span id="cb2-7"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">func</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">format_prompt</span>(motion_desc: String, frames: int) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> String:</span>
<span id="cb2-8">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"&lt;motion&gt;</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%s</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">&lt;/motion&gt;&lt;frames&gt;</span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%d</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">&lt;/frames&gt;"</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%</span> [motion_desc, frames]</span>
<span id="cb2-9"></span>
<span id="cb2-10"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">func</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">decode_output</span>(tokens: PackedInt32Array) <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-&gt;</span> Array:</span>
<span id="cb2-11">    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># motion-bricks specific: tokens -&gt; pose keyframes</span></span>
<span id="cb2-12">    ...</span></code></pre></div></div>
<p>Adapters run under Godot’s script permission system + the existing <code>modules/sandbox/</code> capabilities: an adapter loaded at runtime from disk (or from the SQLite model bundle per RFD 2214) cannot escape the GDScript sandbox, cannot open arbitrary files, cannot spawn processes. Same shape RFD 2213 relies on for VRM.</p>
<p><strong>Layer 3 — game / video code.</strong> Consumers instantiate adapters by resource path, feed them domain input, get domain output. The game never touches ggml types directly.</p>
</section>
<section id="what-this-consolidates" class="level2">
<h2 class="anchored" data-anchor-id="what-this-consolidates">What this consolidates</h2>
<p>Names the RFD 2229 candidates that this RFD acts on:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th>project</th>
<th>before</th>
<th>after</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>motion-bricks-cpp</td>
<td>own C++ module + own ggml pin + own CMake</td>
<td>one shared <code>modules/ggml/</code> + <code>motion_bricks.gd</code> adapter</td>
</tr>
<tr class="even">
<td>kimodo</td>
<td>own C++ module + own ggml pin</td>
<td>shared <code>modules/ggml/</code> + <code>kimodo.gd</code> adapter</td>
</tr>
<tr class="odd">
<td>skin-tokens-cpp</td>
<td>own C++ module + own ggml pin</td>
<td>shared <code>modules/ggml/</code> + <code>skin_tokens.gd</code> adapter</td>
</tr>
</tbody>
</table>
<p>Result: <strong>one native module, N GDScript adapter files</strong>, instead of N per-project C++ modules with parallel build systems and parallel ggml pins.</p>
</section>
<section id="trade-vs-the-risc-v-elf-path" class="level2">
<h2 class="anchored" data-anchor-id="trade-vs-the-risc-v-elf-path">Trade vs the RISC-V ELF path</h2>
<p>RFD 2213 puts godot-vrm inside a sandbox as a compiled ELF via libriscv (<code>modules/sandbox/</code>). Adapter-in-GDScript trades vs adapter-in-ELF:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th></th>
<th>GDScript adapter</th>
<th>RISC-V ELF adapter</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>build</td>
<td>none (script edit + reload)</td>
<td>cross-compile to riscv-elf</td>
</tr>
<tr class="even">
<td>iteration</td>
<td>editor hot-reload</td>
<td>rebuild + repackage</td>
</tr>
<tr class="odd">
<td>third-party untrusted code</td>
<td>can’t run C/Rust</td>
<td>can run any language</td>
</tr>
<tr class="even">
<td>performance</td>
<td>GDScript VM (fine for orchestration)</td>
<td>libriscv (fine for compute)</td>
</tr>
<tr class="odd">
<td>sandbox</td>
<td>Godot script permissions</td>
<td>libriscv memory isolation</td>
</tr>
</tbody>
</table>
<p><strong>Recommended pick per case:</strong></p>
<ul>
<li><strong>Adapter is orchestration + tensor-shape juggling</strong> (tokenizer setup, LoRA scaling factors, chat template, prompt formatting, output post-processing): <strong>GDScript</strong>. This is where all three ggml consumers live today.</li>
<li><strong>Adapter runs third-party untrusted code</strong> (a downloaded planner from a scene marketplace, a mod-supplied model wrapper): <strong>RISC-V ELF via libriscv</strong> (same as RFD 2213’s VRM path).</li>
</ul>
<p>The workspace ships trusted models we hold; GDScript adapters cover every current case. The ELF path stays available for the mod-supplied case if it appears.</p>
</section>
<section id="sequencing" class="level2">
<h2 class="anchored" data-anchor-id="sequencing">Sequencing</h2>
<p>Follow-up L1 RFDs, one per step:</p>
<ol type="1">
<li>Add <code>2-contract/ggml/</code> as a module under <code>entities-godot-sandbox/modules/ggml/</code>. Wire the GDExtension surface. First <code>Ggml.load_model()</code> + <code>Ggml.run_inference()</code> round-trip.</li>
<li>Author <code>motion_bricks.gd</code> adapter. Port whatever’s in motion-bricks-cpp’s adapter code (<code>src/root.cpp</code> planner glue, tokenizer bindings) to GDScript. Verify inference matches the C++ baseline on a fixed motion prompt.</li>
<li>Retract RFD 2212 (motion-bricks-as-native-godot-module) — the module doesn’t exist as an independent thing anymore; its C++ becomes part of <code>modules/ggml/</code>’s WebGPU backend sourcing, its adapter becomes <code>motion_bricks.gd</code>.</li>
<li>Repeat for kimodo and skin-tokens-cpp as separate L1s.</li>
<li>Once all three ggml consumers are on the shared module, trim their standalone C++ repos from the goal manifest.</li>
</ol>
</section>
<section id="verification" class="level2">
<h2 class="anchored" data-anchor-id="verification">Verification</h2>
<ul>
<li><strong>Inference-parity gate.</strong> For each adapter, a fixture prompt round-trips through both the pre-migration C++ path and the post-migration adapter+module path; outputs match byte-for- byte (or within f32 tolerance for stochastic samplers seeded identically).</li>
<li><strong>Adapter-iteration measurement.</strong> Editor reload time on an adapter edit against baseline C++ rebuild-and-relink time. If the ratio isn’t at least 10:1 in GDScript’s favor, the interchangeable-parts win is fictional.</li>
<li><strong>Module-count enumeration.</strong> <code>git ls-files modules/*/config.py</code> under <code>entities-godot-sandbox</code> shows one ggml-consuming module (not three) once migration completes.</li>
</ul>
</section>
<section id="blast-radius" class="level2">
<h2 class="anchored" data-anchor-id="blast-radius">Blast radius</h2>
<p>The three consuming projects (motion-bricks-cpp, kimodo, skin-tokens-cpp) are pre-ship — no downstream depends on their current C++ ABI. This migration doesn’t break anyone external.</p>
<p>Internal churn: RFD 2212’s planned <code>modules/motionbricks/</code> becomes <code>modules/ggml/</code>; RFD 2214’s model bundle loader still returns the same bytes, the caller changes from C++ (<code>mb_model_load_from_memory</code>) to GDExtension (<code>Ggml.load_model_from_bytes</code>).</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>RFD 2188 (one ggml across workspace) — the source consolidation this RFD ships against.</li>
<li>RFD 2211 (base tree entities-godot-sandbox) — the tree the new module lands in.</li>
<li>RFD 2212 (motion-bricks-as-native-godot-module) — this RFD supersedes 2212’s shape; 2212’s L1 execution work becomes Step 2 of this RFD’s sequencing.</li>
<li>RFD 2213 (VRM via godot-sandbox ELF) — the parallel path for cases needing full ELF sandbox rather than GDScript sandbox.</li>
<li>RFD 2214 (model bundle SQLite+ZSTD) — the bundle loader surface adapters call.</li>
<li>RFD 2229 (interchangeable-parts consolidation policy) — the policy this RFD lands one of; the three-C++-module → one- native-module-plus-N-GDScript-adapters shape is exactly what 2229’s ggml-consumers row proposed.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2230-ggml-adapters-in-godot-sandbox/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2229: interchangeable-parts consolidation as workspace policy</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2229-interchangeable-parts-consolidation/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L3 (strategy — sets a policy that touches every future part-adding decision) 2026-09-05 as workspace policy plus name the concrete candidates already visible in the goal manifest future RFDs that add a new module / fork / build fragment / loader path, memory <code>consolidate-interchangeable-parts</code></p>
<section id="the-policy" class="level2">
<h2 class="anchored" data-anchor-id="the-policy">The policy</h2>
<p>Operator directive 2026-09-05, verbatim: <em>“like ford’s factories we have to conslidate our interchagable parts. we have too many parts”</em> + trademark-scrub follow-up <em>“please avoid using the company name ford as a trademark for inventing the interchable parts process”</em>.</p>
<p><strong>Historical note.</strong> Interchangeable parts predate the company that got named in the first draft by a century — Whitney’s musket contract 1798, the Springfield Armory / American System of manufactures through the 1820s–1850s. That company scaled assembly-line production using interchangeable parts; it did not invent them. Neutral vocabulary is <strong>interchangeable parts</strong>, <strong>standardized interfaces</strong>, <strong>the American System of manufactures</strong> if a historical anchor is wanted.</p>
<p><strong>The workspace rule (goes into CLAUDE.md next):</strong></p>
<p>Before landing a new part — a module, a fork, a build fragment, a loader path, a bundle format, a C API extension — check whether an existing part covers the interface. If yes, extend the existing part rather than land a parallel one. Every RFD that names a new component carries a sentence naming the existing part it substitutes for and why substituting doesn’t work. “Doesn’t work” is a measurement, not a preference.</p>
<p>Two mechanisms already carry the rule for specific interfaces:</p>
<ul>
<li><code>2-contract/ggml/</code> (RFD 2188) as the single ggml source every consumer links.</li>
<li><code>2-contract/manuals-weftspun/</code> (this repo) as the single workspace-doctrine mount, reached from <code>weftspun-keypoint</code> via linkfile.</li>
</ul>
</section>
<section id="live-candidates-in-the-manifest-2026-09-05-as-of-writing" class="level2">
<h2 class="anchored" data-anchor-id="live-candidates-in-the-manifest-2026-09-05-as-of-writing">Live candidates in the manifest (2026-09-05 as of writing)</h2>
<p>Enumerated from <code>weftspun-keypoint/default.xml</code> at 112 project entries.</p>
<section id="three-ggml-checkouts" class="level3">
<h3 class="anchored" data-anchor-id="three-ggml-checkouts">Three <code>ggml</code> checkouts</h3>
<pre><code>3-interactor/trellis2cpp/ggml       remote=weftspun  rev=331b9cba
3-interactor/ggml-seethrough        remote=weftspun  rev=3404c951
2-contract/ggml                     remote=weftspun  rev=weftspun-consolidated</code></pre>
<p><strong>RFD 2188 named <code>2-contract/ggml</code> as the single ggml source workspace-wide.</strong> The manifest still ships three checkouts. Two of them (<code>trellis2cpp/ggml</code>, <code>ggml-seethrough</code>) are consumers still pinned to their own ggml revisions rather than reading through the shared source.</p>
<p><strong>Verification needed before trimming.</strong> Whether each consumer has actually migrated to <code>2-contract/ggml</code>’s API, or whether the pinned revision holds something the consolidated source doesn’t yet cover. A CI green on <code>trellis2-ex</code> and <code>seethrough</code> against <code>2-contract/ggml</code> is the measurement.</p>
<p>Follow-up L1 RFDs (planned in the 22xx range): one per consumer scoping the migration + trim per project.</p>
</section>
<section id="entities-godot-main-trimmed-2026-09-05" class="level3">
<h3 class="anchored" data-anchor-id="entities-godot-main-trimmed-2026-09-05"><code>entities-godot-main</code> (trimmed 2026-09-05)</h3>
<p>Same repo as <code>entities-godot-sandbox</code> at revision <code>main</code> instead of <code>feat/vsk-sandbox-4.7</code>. Trimmed as <code>weftspun-keypoint#101</code>. Recorded here as the reference case that proves the pattern lands cleanly.</p>
</section>
<section id="motion-bricks-cpp-kimodo-skin-tokens-cpp" class="level3">
<h3 class="anchored" data-anchor-id="motion-bricks-cpp-kimodo-skin-tokens-cpp"><code>motion-bricks-cpp</code> + <code>kimodo</code> + <code>skin-tokens-cpp</code></h3>
<p>Three ggml-graph-in-C++ shapes with three separate build systems. All target the same interface: a C API that Godot’s <code>modules/motionbricks/</code> (RFD 2212) wraps for scene-graph access. Consolidation candidate: one <code>ggml-godot-module-kit</code> that all three link, replacing three per-project CMake <code>if(NATIVE_WEBGPU)</code> branches with one shared fragment.</p>
<p>Not urgent; the three projects are early enough that their build systems haven’t diverged much. RFD 2188 shared source + a small build-fragment consolidation covers most of it. Concrete work is a follow-up L2 once RFD 2212’s <code>modules/motionbricks</code> lands.</p>
</section>
<section id="manifests-of-manifests" class="level3">
<h3 class="anchored" data-anchor-id="manifests-of-manifests">Manifests-of-manifests</h3>
<p><strong>Not a candidate.</strong> <code>weftspun-keypoint</code> is the single live goal manifest per CLAUDE.md’s “Sides” rule; the archived <code>weftspun-mesh-latents</code> is placement history, not a parallel manifest.</p>
</section>
</section>
<section id="verification" class="level2">
<h2 class="anchored" data-anchor-id="verification">Verification</h2>
<p>The policy is measured by:</p>
<ol type="1">
<li><strong>RFD-add gate.</strong> A new RFD that adds a component but does not name an existing part it substitutes for is asking for a parallel part. Add a section to <code>check_rfd_structure.py</code> requiring an “Alternatives considered” or “Substitutes for” paragraph when the RFD’s frontmatter is tagged as adding a module/fork/bundle-format. Draft in a follow-up PR; RFD 1000 carries the structure list this gate reads from.</li>
<li><strong>Manifest-scan gate.</strong> <code>scripts/check_manifest_dupes.py</code> (new) reads the live goal manifest, groups by name/path/ remote+rev, and reports groups of size &gt; 1 as candidates needing a doctrine pointer (an RFD saying why both stay) or a trim PR (like <code>weftspun-keypoint#101</code>). Silent-skip case: the two-manifest state that exists during a manifest cutover — the gate reports both, doesn’t fail on the presence.</li>
</ol>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>RFD 2188 (one ggml across workspace) — the reference consolidation this RFD generalises.</li>
<li>RFD 2210 (L3 atelier shipping surface) — the arc that produced the directive.</li>
<li>RFD 2211 (base tree entities-godot-sandbox) — picked the tree the trim in <code>weftspun-keypoint#101</code> acted on.</li>
<li>Memory <code>consolidate-interchangeable-parts</code> — the operator directive + how-to-apply, kept as the working memory next to this policy.</li>
<li>Memory <code>coordinator-verify-before-relay</code> — records the earlier trademark violation this RFD’s naming was scrubbed against.</li>
</ul>
</section>
<section id="operator-context-2026-09-05" class="level2">
<h2 class="anchored" data-anchor-id="operator-context-2026-09-05">Operator context 2026-09-05</h2>
<p>Two verbatim directives (paired):</p>
<blockquote class="blockquote">
<p>like ford’s factories we have to conslidate our interchagable parts. we have too many parts</p>
</blockquote>
<blockquote class="blockquote">
<p>please avoid using the company name ford as a trademark for inventing the interchable parts process</p>
</blockquote>
<p>Both applied. The first is the policy; the second is the naming. The RFD carries neutral vocabulary from here on.</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2229-interchangeable-parts-consolidation/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2216: Three.js blocklist</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2216-threejs-blocklist/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L2 (coordination — closes a runtime fork) blocklist as an in-browser 3D runtime in consumer files</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Blocklist three.js as an in-browser 3D runtime. Substitute is <strong>Godot as a native binary per platform</strong> built from <code>entities-godot-sandbox</code> with the Vulkan renderer (MoltenVK on macOS). Three.js itself is MIT-licensed — the objection is not licence, it is runtime story: the workspace ships every 3D surface via Godot, and a three.js path forks the scene-graph, material pipeline, animation graph, and lighting model.</p>
<p>Live rows: <code>CLAUDE.md</code> blocklist table + <code>BLOCKLIST.md</code> full section (the section body carries the argument in current form).</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><a href="../2210-atelier-godot-web-shipping-surface/">RFD 2210</a> — L3 strategic bet.</li>
<li><a href="../2211-base-tree-entities-godot-sandbox/">RFD 2211</a> — base tree pick.</li>
<li><a href="../2215-one-binary-two-heads/">RFD 2215</a> — the “one runtime, not two” argument that also drives this blocklist.</li>
<li>RFD 1170 (cleanroom presence loop) — earlier RFD picking Godot over three.js for a different workload.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2216-threejs-blocklist/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2215: one binary, two heads</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2215-one-binary-two-heads/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L2 (coordination) capture head) and the game (interactive head) muxing for the capture head</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>One source tree, one CI workflow, one native binary per platform (macOS / Windows / Linux), two invocation flags:</p>
<ul>
<li><strong>Head A — game.</strong> Native window; loads Starforged fixture (<code>starforged.sqlite</code>), calls <code>taskweft</code>’s planner (RFD 2205), surfaces the decision-point menu via a Godot Control-node VN layout, plays reactions on the VRM portrait via <code>Ggml.run_inference()</code> (RFD 2230) for motion + VRM expressions for face.</li>
<li><strong>Head B — marketing video.</strong> Headless render pass of the same binary. <code>godot --headless --write-movie shot&lt;NN&gt;.&lt;container&gt; ...</code> captures the runway scene per shot; video muxed via CineForm per RFD 1123 (ffmpeg blocklisted, see memory <code>ffmpeg-blocklisted</code>).</li>
</ul>
<p>Same <code>.tscn</code> / <code>.tres</code> assets, same binary, different invocation flag.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><a href="../2210-atelier-godot-web-shipping-surface/">RFD 2210</a> — L3 strategic bet.</li>
<li><a href="../2216-threejs-blocklist/">RFD 2216</a> — same “one runtime, not two” argument.</li>
<li>RFD 1123 (CineForm in Godot) — Head B’s encoder.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2215-one-binary-two-heads/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2214: model bundle as ZSTD-compressed SQLite on local disk</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2214-model-bundle-sqlite-range-fetch-zstd/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L2 (coordination) native binary at runtime per-consumer adapter files</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Ship the model in a ZSTD-compressed SQLite file on local disk, next to the native binary. Loader is <code>sqlite3_open()</code> on the filesystem path; the GDExtension surface RFD 2230 defines (<code>Ggml.load_model()</code>) reads it and hands bytes to ggml. Two schema shapes:</p>
<ul>
<li><strong>Shape A</strong> (v1): whole GGUF as one BLOB in <code>model_weights(id, gguf)</code>. Simplest ship path.</li>
<li><strong>Shape B</strong> (v2, if measurement asks for it): one row per tensor, <code>tensors(name, shape, dtype, offset, blob)</code>. Enables never-fully- resident-in-memory models on constrained hosts.</li>
</ul>
<p>ZSTD-compress at the SQLite-page level. A gate lands to enforce it.</p>
<p>Files live in the native binary’s install directory (<code>godot/data/models/&lt;name&gt;.zstd.sqlite</code>) or under <code>$XDG_DATA_HOME/atelier/models/</code>; downloaded at install time via the release channel that ships the binary.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><a href="../2210-atelier-godot-web-shipping-surface/">RFD 2210</a> — L3.</li>
<li><a href="../2229-interchangeable-parts-consolidation/">RFD 2229</a> — the SQLite+ZSTD bundle format is the shared model-delivery interface across ggml consumers.</li>
<li><a href="../2230-ggml-adapters-in-godot-sandbox/">RFD 2230</a> — the <code>Ggml.load_model()</code> GDExtension surface that consumes the bundle.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2214-model-bundle-sqlite-range-fetch-zstd/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2213: VRM 1.0 loading via godot-sandbox RISC-V ELF</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2213-vrm-via-godot-sandbox-elf/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L2 (coordination) without adding new C++ or forking upstream <code>V-Sekai/godot-vrm</code></p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Compile <code>V-Sekai/godot-vrm</code> (GDScript addon) to a RISC-V ELF via <code>godot-sandbox</code>’s toolchain. Ship at <code>res://addons/godot-vrm/godot-vrm.elf</code>. Load at runtime through <code>modules/sandbox</code> (libriscv). Godot’s <code>GLTFDocument</code> + <code>GLTFDocumentExtension</code> API hands raw VRM bytes to the ELF; the ELF returns node/skeleton/expression/spring-bone data.</p>
</section>
<section id="rejected" class="level2">
<h2 class="anchored" data-anchor-id="rejected">Rejected</h2>
<ul>
<li><strong>New C++ <code>modules/vrm/</code></strong> subclassing <code>GLTFDocumentExtension</code> — duplicates work already done in godot-vrm’s GDScript. Possible follow-up when sandbox overhead measurably hits latency.</li>
<li><strong>Bundle godot-vrm as a plain GDScript addon</strong> — parsing runs in the main GDScript VM; no isolation from a malicious VRM.</li>
</ul>
</section>
<section id="cascade" class="level2">
<h2 class="anchored" data-anchor-id="cascade">Cascade</h2>
<p><code>modules/sandbox</code> (libriscv) is on the KEEP list because its ELF-execution capability is what makes this work.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><a href="../2210-atelier-godot-web-shipping-surface/">RFD 2210</a> — L3.</li>
<li><a href="../2211-base-tree-entities-godot-sandbox/">RFD 2211</a> — the base-tree choice.</li>
<li><a href="../2230-ggml-adapters-in-godot-sandbox/">RFD 2230</a> — parallel path for ggml adapters (GDScript-in-sandbox for orchestration, ELF-in-sandbox for third-party untrusted code).</li>
<li>RFD 2206 (video-call VRM portrait) — uses this ELF path for the VRM LookAt + expression node.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2213-vrm-via-godot-sandbox-elf/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2211: base tree — entities-godot-sandbox for the atelier</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2211-base-tree-entities-godot-sandbox/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L2 (coordination)</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p><code>entities-godot-sandbox</code> is the atelier base tree. It carries local <code>modules/sandbox/</code> modifications libriscv needs for the ELF loader path (RFD 2213 loads godot-vrm as a sandboxed ELF, RFD 2230 loads GDScript ggml adapters under Godot’s script sandbox). Pin the exact commit in <code>.repo/manifests/default.xml</code>.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><a href="../2210-atelier-godot-web-shipping-surface/">RFD 2210</a> — L3.</li>
<li><a href="../2213-vrm-via-godot-sandbox-elf/">RFD 2213</a> — sandbox ELF loader.</li>
<li><a href="../2229-interchangeable-parts-consolidation/">RFD 2229</a> — consolidation policy.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2211-base-tree-entities-godot-sandbox/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2210: atelier shipping surface</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2210-atelier-godot-web-shipping-surface/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><strong>Flight level:</strong> L3 (strategy, portfolio bet — see RFD 2177) one runtime, two heads (interactive + headless capture) L2 fanout at RFDs 2211/2214/2215/2216, adapter shape at RFD 2230</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>One runtime, one binary per platform (macOS / Windows / Linux) from <code>3-interactor/entities-godot-sandbox</code>. Vulkan renderer (MoltenVK on macOS). Two heads: interactive window for the Starforged VN game, headless <code>godot --headless --write-movie</code> for the marketing video. Same <code>.tscn</code> / <code>.tres</code> assets, same binary, different invocation flag. ggml consumers ride on one shared <code>modules/ggml/</code> module (RFD 2230) with per-model GDScript adapters. Three.js is blocklisted (RFD 2216). SQLite + ZSTD is the model bundle format on local disk (RFD 2214). Video muxed through CineForm per RFD 1123.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><a href="../2211-base-tree-entities-godot-sandbox/">RFD 2211</a> — base tree.</li>
<li><a href="../2214-model-bundle-sqlite-range-fetch-zstd/">RFD 2214</a> — model bundle format.</li>
<li><a href="../2215-one-binary-two-heads/">RFD 2215</a> — two-heads shape.</li>
<li><a href="../2216-threejs-blocklist/">RFD 2216</a> — three.js blocklist.</li>
<li><a href="../2229-interchangeable-parts-consolidation/">RFD 2229</a> — consolidation policy.</li>
<li><a href="../2230-ggml-adapters-in-godot-sandbox/">RFD 2230</a> — ggml as one shared module + GDScript adapters.</li>
<li>RFD 2205 (Taskweft in Bao), RFD 2206 (video-call VRM portrait), RFD 2207 (Nord palette), RFD 2188 (one ggml across workspace), RFD 1123 (CineForm in Godot), RFD 1170 (presence loop).</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2210-atelier-godot-web-shipping-surface/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2209: CC-BY-NC as a blocklist entry</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2209-cc-by-nc-blocklist-entry/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>alongside <code>CC-BY-SA</code>, and gate build scripts to drop CC-BY-NC rows before shipping any derived corpus document; today’s concrete case is <code>scripts/build_starforged_hf.py</code> and <code>build_starforged_sqlite.py</code> against the dataforged repo</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Add a row to CLAUDE.md’s “Blocklists” table:</p>
<pre><code>| **CC-BY-NC (all versions)** | non-commercial restriction propagates into anything derived from the row — same downstream-risk shape as CC-BY-SA — see below |</code></pre>
<p>with a corresponding section in <code>BLOCKLIST.md</code> argued below. The row is enforced two places:</p>
<ol type="1">
<li><strong>Build-script filter.</strong> Any script that ingests a mixed-licence source reads each row’s licence field and drops CC-BY-NC rows before the parquet or SQLite write. Reports the drop count as a rule-3 named-not-silent gate.</li>
<li><strong>CI on manuals-weftspun.</strong> <code>scripts/check_starforged_hf.py</code> asserts <code>every row's licence == CC-BY-4.0</code> (or another commercial-clean licence the workspace already accepts). A CC-BY-NC row surviving the filter fails CI.</li>
</ol>
<p>The rule does NOT ban <em>reading</em> CC-BY-NC content locally for inspection. It bans CC-BY-NC rows from shipping in a derived corpus, a training dataset, a demo fixture, or any artifact this workspace publishes.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>Concrete case that produced this RFD: <strong>dataforged</strong> — the community JSON rendering of the Starforged and Ironsworn SRDs at <code>github.com/rsek/dataforged</code> — carries a mixed licence:</p>
<ul>
<li><code>dist/starforged/*.json</code> — CC-BY-4.0. Ships.</li>
<li><code>ironsworn/</code> subtree — CC-BY-NC-4.0. Filtered out.</li>
<li>Raster illustrations — CC-BY-NC-4.0. Filtered out.</li>
</ul>
<p>Without the filter, <code>chibifire/starforged</code> on HuggingFace would have shipped CC-BY-NC rows mixed with CC-BY-4.0 rows under a single licence declaration. That is a licence-provenance failure for downstream consumers who take the workspace at its word that <code>chibifire/starforged</code> is CC-BY-4.0. The filter closes it. The row on CLAUDE.md’s blocklist ensures the <em>next</em> mixed-licence source (dataforged is not going to be the last) inherits the same handling by default rather than by anyone remembering.</p>
<p>The failure mode is the same as CC-BY-SA’s — a share-alike or non-commercial restriction that propagates into anything derived from the row. Different clause, same downstream shape. The CC-BY-SA row already on the blocklist is the argument this row mirrors.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not a rule against every restrictive licence — CC0, MIT, Apache, CC-BY-4.0, and the other permissive licences the workspace already accepts continue to. Not a rule against downloading CC-BY-NC content for personal inspection (an operator reading the Ironsworn PDF is fine; a corpus builder ingesting its text is not). Not a rule against forking or citing CC-BY-NC works — attribution of a source is different from shipping its content.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>CLAUDE.md’s existing <code>CC-BY-SA</code> row — same shape, same argument.</li>
<li>RFD 2205 (Taskweft in Bao) — the Starforged play surface whose corpus is the concrete case.</li>
<li>Codebase: <code>2-contract/manuals-weftspun/scripts/build_starforged_hf.py</code> and <code>build_starforged_sqlite.py</code> — the reference filter implementations.</li>
<li>Codebase: <code>2-contract/manuals-weftspun/scripts/check_starforged_hf.py</code> — the downstream CI assertion.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2209-cc-by-nc-blocklist-entry/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2209-cc-by-nc-blocklist-entry/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2207: Nord palette (or peer) for shipped demos</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2207-nord-palette-for-demos/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>palette (Nord as the default, Solarized / Catppuccin / Tokyo Night / Rose Pine / Gruvbox as acceptable peers) rather than the warm amber-on-panel look that Claude artifacts default to every Artifact this workspace publishes as a deliverable; the same rule does not bind private one-shot artifacts an operator uses to inspect an intermediate</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>A demo or artifact that ships as a reviewable surface picks one of the following six FOSS-licensed palettes verbatim and sources its tokens from that palette’s published spec:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>palette</th>
<th>licence</th>
<th>source</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Nord</td>
<td>MIT</td>
<td>nordtheme.com</td>
</tr>
<tr class="even">
<td>Solarized</td>
<td>MIT</td>
<td>ethanschoonover.com/solarized</td>
</tr>
<tr class="odd">
<td>Catppuccin</td>
<td>MIT</td>
<td>catppuccin.com</td>
</tr>
<tr class="even">
<td>Tokyo Night</td>
<td>MIT</td>
<td>github.com/enkia/tokyo-night-vscode-theme</td>
</tr>
<tr class="odd">
<td>Rose Pine</td>
<td>MIT</td>
<td>rosepinetheme.com</td>
</tr>
<tr class="even">
<td>Gruvbox</td>
<td>MIT</td>
<td>github.com/morhetz/gruvbox</td>
</tr>
</tbody>
</table>
<p>Nord is the default this session’s Starforged surface adopted and what a new demo picks in the absence of a reason to pick otherwise. The five peers are named so the choice is not hardcoded to one system — a demo whose subject matter reads better in warm tones can pick Gruvbox; a demo tuned for a night-shift reader can pick Tokyo Night. What is banned is the un-tokenised amber-on-panel default that a first-draft artifact carries when nothing was picked.</p>
<p>Both light and dark variants are declared. <code>data-theme="dark"</code> and <code>data-theme="light"</code> on the root, plus a <code>prefers-color-scheme</code> media query, per the artifact-design contract.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The mid-session operator redirect that produced the Starforged demo’s Nord palette was blunt: the warm-terminal look that Claude artifacts default to reads as an AI intermediate, not as a deliverable. A reviewer who opens a shipped demo and sees the default Claude look concludes, correctly, that nobody picked a palette. Picking one — any of the six above — closes that reading.</p>
<p>The rule is about <em>picking</em> rather than about <em>which one</em>. What the existing default fails at is being un-considered; the six peers all pass by virtue of being considered and readable.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not a component library or a spacing/typography spec. Not a mandate to reskin dot-claude, existing internal tooling, or operator-private artifacts. Not a mandate that every demo pick Nord specifically — the peers exist for cases where a different palette reads better on the subject.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>RFD 2206 (video-call VRM portrait) — the shipped surface this palette was first applied to.</li>
<li>Codebase: <code>7-service/service-sqlar-cas/docs/index.html</code> — the reference Nord application in this workspace.</li>
<li>CLAUDE.md’s “Trademarks Stay Out of Shipping Artifacts” clause — the six palettes are all trademark-clean and named by their own project names.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2207-nord-palette-for-demos/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2207-nord-palette-for-demos/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2206: Video-call VRM portrait convention</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2206-video-call-vrm-portrait/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>FaceTime-style portrait — fixed eye-level camera, LookAt on the camera with periodic glance-away, breathing sway, blink cycle, reaction blendshapes that fade over about the width of a golf ball of time (~1.6 s) as an interlocutor rather than an inhabitant of a world; today’s concrete case is the Starforged play surface in <code>7-service/service-sqlar-cas/docs/</code></p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>A VRM presented as an interlocutor is staged as <strong>a 3D character in a small diorama, framed like a video call</strong>. The frame is fixed; the character animates within it; there is no world navigation. Concretely:</p>
<ol type="1">
<li><strong>Camera.</strong> Eye-level (Head bone Y, ~1.52 m off the floor for an adult VRM — about the height of an adult wrist reached overhead), offset ~0.55 m along Z (about eight stacked AA batteries), no downward pitch. Targets a point just below the eyes so head + shoulders + up-to-the-ribcage fill the frame. Portrait 4:5 aspect (phone-call-native).</li>
<li><strong>LookAt.</strong> The VRM’s <code>LookAt</code> target follows the camera every frame via <code>godot-vrm</code>’s <code>VRMTopLevel</code> LookAt proxy (loaded as a sandbox ELF per <a href="../2213-vrm-via-godot-sandbox-elf/">RFD 2213</a>). The player IS the caller. A small time-based glance-away every 4-7 s for ~300 ms (about the width of a pencil of time) prevents the death-stare failure mode.</li>
<li><strong>Idle motion.</strong> Ambient sway on <code>Spine</code> / <code>UpperChest</code> at 0.02 rad amplitude with a 4-s sinusoid for breathing. Blink cycle ~4 s period with 100 ms closed.</li>
<li><strong>Reactions.</strong> A move outcome (<code>strong-hit</code> / <code>weak-hit</code> / <code>miss</code>) fires a VRM expression blendshape (<code>happy</code> / <code>neutral</code> / <code>sad</code>) that holds for ~1.5 s then decays over ~100 ms back to neutral. Total envelope ~1.6 s, about the width of a golf ball of time. Reuses the VRM expression node <code>godot-vrm</code> exposes.</li>
<li><strong>Diorama backdrop.</strong> A low-poly scene-flavored backdrop behind the character, swappable per-scene from a small <code>scenes/*.gltf</code> set. Kept deliberately low-detail so the character reads as the subject.</li>
<li><strong>What is banned.</strong> No WASD, no arrow-key movement, no pointer-lock mouse look, no wheel zoom, no gamepad stick reads, no <code>avatar.pos</code> mutation, no <code>orbit.yaw / pitch / dist</code> camera state. A demo that presents a VRM as an interlocutor and lets the player fly the camera around it undoes the convention.</li>
</ol>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The reflex-executor demo that shipped in <code>7-service/service-sqlar-cas/docs/</code> first framed its VRM as an inhabitant of a scene — WASD, orbit camera, gamepad. The Starforged retarget of the same page kept the WASD wiring alive, and the character read as a doll on a stage rather than as the person the player was talking to. The retarget was a <em>decision-point</em> game where the player is in dialogue with a character; the video-call frame is what makes that reading legible without any extra prose. This RFD writes that legibility down as a convention future character-facing demos default to.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not a convention for third-person action, exploration, or world demos — those keep their world navigation. Not a rig spec (the underlying VRM is unchanged). Not a scene-content spec (dioramas are per-demo; only that they exist and stay low-detail is mandated).</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>RFD 1170 (presence loop) — this RFD is its visual convention.</li>
<li>RFD 2205 (Taskweft in Bao) — the Starforged play surface that this convention was first applied to.</li>
<li><a href="../2213-vrm-via-godot-sandbox-elf/">RFD 2213</a> — how the VRM loads in the current native Godot binary (godot-vrm ELF via libriscv sandbox); the earlier <code>@pixiv/three-vrm</code> reference implementation at <code>7-service/service-sqlar-cas/docs/vrm.js</code> is retired with the three.js blocklist (RFD 2216).</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2206-video-call-vrm-portrait/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2206-video-call-vrm-portrait/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2205: Taskweft in Bao — database plugin, cgo-linked</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2205-taskweft-in-bao/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>database-plugin-type Bao plugin (Go binary, cgo-linked C++ from <code>taskweft_nif/standalone/</code>); the same C++ compiles via emcc to a <code>taskweft.wasm</code> blob callable from the browser loads it; the git-tracked fleet.jsonld → fleet.sqlite mirror pipeline; the browser-side WASM parity target</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Ship one C++ planner (<code>taskweft_nif/standalone/*.hpp</code>, unchanged), one thin <code>extern "C"</code> shim mirroring its 23-function NIF surface, and two hosts that call the shim:</p>
<ol type="1">
<li><strong>Bao plugin</strong> — Go binary at <code>7-service/service-taskweft-bao/</code>, registered against weftspun-bao as <code>bao plugin register database taskweft</code>. Every peer resolves “what is my next task?” as <code>bao read taskweft/creds/&lt;goal_id&gt;</code>. Bao stamps the response <code>lease_id</code> on the server clock — every peer sees the same <code>expires_at</code>, so global time for durations is satisfied by construction. <code>may-use--&lt;device&gt;</code> ReBAC tuples become ENFORCED at read-time via Bao’s own capability check (fills the compute- lease broker gap RFD 2202 named as future work).</li>
<li><strong>CDN + WASM</strong> — emcc-built <code>taskweft.wasm</code> loaded from GitHub Pages, reading a static <code>fleet.sqlite</code> via HTTP <code>Range: bytes=0-</code> the same way <code>service-sqlar-cas/docs/</code>’s persona demo already loads <code>persona.sqlite</code>. No server, no toolchain — reviewers see the planner’s output client-side against the same fixture the Bao plugin runs.</li>
</ol>
<p>The same <code>.sqlite</code> fixture ships under <code>docs/fixtures/</code> on GitHub Pages and is the file the Bao plugin reads from <code>secret/taskweft/db/fleet</code> after CI’s mirror step. <strong>One file, two hosts, one planner.</strong></p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>RFD 2204 named the fleet-coordination shape; the plan-mode session of 2026-09-05 iterated three approaches before landing here. Two earlier shapes are retracted, deliberately — retraction pointers below.</p>
<p>Peer sessions today coordinate through Bao rows + free-form <code>SendMessage</code> + operator-typed un-park signals. There is no shared representation of <em>why</em> a peer is doing what it is doing, no programmatic assignment surface, no compute-lease enforcement, and no clock the whole fleet can agree on for lease <code>expires_at</code>. This RFD is the answer.</p>
</section>
<section id="retractions" class="level2">
<h2 class="anchored" data-anchor-id="retractions">Retractions</h2>
<ul>
<li><strong>coordinator.ex Elixir adapter as primary:</strong> retracted 2026-09-05, superseded by the Go plugin in this RFD. The Elixir standalone adapter had no answer for global time or ReBAC enforcement.</li>
<li><strong>thin-HTTPS bridge to taskweft-mcp.fly.dev:</strong> retracted 2026-09-05, superseded by cgo-linked C++. The bridge shape added an external service dependency the plugin doesn’t need; embedding the planner satisfies operator’s constraint that the plugin requires only REST commands and no other service.</li>
<li><strong><code>router.ex</code> mTLS auth extension:</strong> retracted with the bridge — nothing calls MCP any more.</li>
</ul>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not a pure-Go port of the planner (RFD 0006 sized it at ~7000 LOC and 8–12 weeks; cgo is 3–5 days). Not a rewrite of the RECTGTN schema. Not a scheduler; not a hook. The plugin does not extend Bao’s storage surface — plan and skip state live in a SQLite blob served by the sibling sqlite-fdb secrets engine, not in KV rows under a separate mount.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>RFD 2204 (RECTGTN fleet coordination) — the domain document shape the plugin queries. This RFD supersedes its “coordinator adapter” section with a one-line pointer, per CLAUDE.md doctrine.</li>
<li>RFD 2140 (OpenBao on FoundationDB) — the storage backend the plugin’s SQLite fixture sits on top of.</li>
<li>RFD 2142 (Bao PKI zerotrust) — the cert-auth path peers use.</li>
<li>RFD 2146 (Bao is the secret store) — the policy discipline any new mount inherits.</li>
<li>RFD 2147 (Bao is critical infrastructure) — the blast-radius bar a new plugin must meet.</li>
<li>RFD 2195 (weftspun-bao Tailscale sidecar) — the deploy shape this plugin ships alongside.</li>
<li>RFD 2202 (ReBAC Bao enforcement) — this RFD fills the compute-lease broker gap RFD 2202 named as future work.</li>
<li>Sibling plugin scaffold: <code>7-service/service-bao-sqlite-fdb/</code> — the layout the new plugin copies verbatim.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2205-taskweft-in-bao/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2205-taskweft-in-bao/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2204: RECTGTN fleet coordination</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2204-rectgtn-fleet-coordination/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>query against one shared fleet domain, rather than by reading prose relayed from the coordinator ceremony (RFD 2201); the Taskweft engine already in tree; a new fleet-domain document</p>
<p><strong>Coordinator adapter as an Elixir module:</strong> retracted 2026-09-05, see <a href="../2205-taskweft-in-bao/README.md">RFD 2205</a> — superseded by an in-Bao database-plugin (Go binary, cgo-linked C++ planner) that callers reach via <code>bao read taskweft/creds/&lt;goal_id&gt;</code>. This RFD’s “fleet-domain document” scope stays live; the “adapter” scope moves to 2205.</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Feed the Taskweft engine a <strong>fleet domain</strong> — a single JSON-LD document conforming to <code>3-interactor/taskweft/priv/schemas/rectgtn_domain.schema.json</code> — that names the live peers as entities, their owned resources as capability edges, the verbs peers execute as actions, and known decompositions as methods. Every peer answers “what should I do next?” with the same call: <code>Taskweft.plan(fleet, actor: self_cn)</code>. Contention becomes capability tuples; ordering becomes ISO-8601 durations; peer authority becomes the ReBAC relations Taskweft already speaks (<code>HAS_CAPABILITY</code>, <code>OWNS</code>, <code>IS_MEMBER_OF</code>, <code>DELEGATED_TO</code>, <code>SUPERVISOR_OF</code>, <code>PARTNER_OF</code>, <code>CAN_ENTER</code>, <code>CAN_INSTANCE</code>). No new planner, no new SQL layer, no port of RECTGTN into Bao; the engine is the ground-truth solver and the fleet is a domain that feeds it.</p>
<p>The seven-step coordinate-agents ceremony (RFD 2201) stays. One line of step 3 (“notify peers”) becomes <code>Coordinator.snapshot |&gt; Coordinator.pick(self) |&gt; Coordinator.publish(self)</code>. Determinism against the shared snapshot removes the need to relay next-item picks in prose. Assignment rows carry a <code>SqlarCas.Caveat</code> with <code>{"type":"expires_at","at":start+duration}</code> — the compute-lease broker RFD 2202 named as future work, filled by reusing the caveat primitive that already exists in service-sqlar-cas.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>Peer sessions coordinate through three ad-hoc channels: Bao rows, free- form <code>SendMessage</code> prose, and operator-typed un-park signals. There is no shared representation of <em>why</em> a peer is doing what it is doing, so a peer that reranks does it by hand, a peer that finishes an item picks the next one from a board it interprets alone, and the operator carries the priority ordering in their head each turn. The <code>may-use--&lt;device&gt;</code> tuples RFD 2202 introduced are documentation only — nothing gates work on them. When two peers want the same GPU or the same HF repo, prose adjudicates.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not a scheduler; not a hook; not a replacement for the operator’s right to reprioritise mid-turn. The fleet domain is a snapshot the operator edits like any other document — the planner is deterministic against whatever the snapshot says. Not a claim that RECTGTN scales to 100+ peers today; the pilot is single-Goal, single-decomposition, and the engine’s search bounds already surface a “no assignable Task” answer rather than looping.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li>RFD 2200 (ReBAC agent roles) — supplies the relation vocabulary the fleet domain reuses.</li>
<li>RFD 2201 (coordinate-agents ceremony) — step 3 gets the one-line hook; the other six steps are unchanged.</li>
<li>RFD 2202 (ReBAC Bao enforcement) — this RFD fills the compute-lease broker gap RFD 2202 named as future work.</li>
<li>RFD 2195 (weftspun-bao) — the Bao row shape the coordinator reads from and writes back to.</li>
<li>Taskweft engine RFDs 0005 (unify capabilities with ReBAC) and 0008 (MCP public API) — the surfaces the fleet uses without modification.</li>
</ul>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2204-rectgtn-fleet-coordination/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2204-rectgtn-fleet-coordination/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2203: The first-subset row shape for the anny-soma keypoint corpus</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2203-anny-soma-first-subset-corpus-shape/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p><code>weftspun-keypoint</code> manifest PR on the 6-datasource side, owned by the GPU-grant holder)</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>The first subset publishes a hybrid row: <code>image</code>, <code>camera</code>, <code>anny_posed_vertices</code> (19,158 × 3 float32, makehuman topology via <code>rig="soma"</code> + <code>remove_unattached_vertices=False</code>; a shard whose vertex count differs is rejected by the manifest gate), <code>keypoints_2d</code> (133 × 3, baked at the <code>wholebody133.pth</code> hash), <code>soma_pose</code> (77 rotvecs; anny prepends one root at call time), and a per-shard manifest carrying the <code>wholebody133.pth</code> SHA-256, the observed SOMA joint count, the motion source, the sampler configuration, and the render seed. Camera <code>sphere_hammersley_sequence</code> per CLAUDE.md. Publish path RFD 2196 rule 5: LFS + <code>hf_transfer</code>, xet disabled, via <code>hf upload-large-folder</code> for per-file resumable commits. Additive subsets and motion refinements each land as their own commit set with the prior commit hash cited as retraction-in-place. Blinded holdout untouched. <code>DETAILS.md</code> carries the tradeoff, the SOMA hook, the RFD 1122 lineage, and open knobs.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>Task #67’s living-dataset ANNY-SOMA corpus needs a schema pick before whoever holds the GPU grant can render, and the pick closes the immutability-vs-agility tradeoff two single-shape options force. Vertex-only rows are label-scheme-neutral but the RFD 2196 viewer cannot overlay keypoints from vertices and the <code>.pth</code> version becomes an implicit training-time dependency. Baked keypoints are viewer-friendly but freeze the current <code>.pth</code>; hybrid holds both.</p>
</section>
<section id="references" class="level2">
<h2 class="anchored" data-anchor-id="references">References</h2>
<p>RFD 2196 (publish rules), RFD 1173 (multimodal pipeline), CLAUDE.md camera-sequence rule.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>Rules originated in RFD 1122 (state abandoned), carried forward by RFDs 1121, 1123, 1126, 1128 and by CLAUDE.md. Consumer is task #65 (HERO’s CUDA-only rescope). Anchor source is <code>wholebody133.pth</code> at <code>weftspun/anny-keypoint-anchors</code> main. SOMA joint-count hook is task #76.</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2203-anny-soma-first-subset-corpus-shape/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2203-anny-soma-first-subset-corpus-shape/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2202: ReBAC role tuples enforced via Bao identity groups</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2202-rebac-bao-enforcement/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Bao <strong>identity groups</strong> enforce the ReBAC role tuples RFD 2200 landed. One group per role, each carrying role-scoped policies; agent entities as members. <code>scripts/sync_rebac_groups.py</code> reads <code>relationships/&lt;agent&gt;--role--&lt;role&gt;</code> and reconciles group memberships. See <code>DETAILS.md</code> for the mapping, reconciler contract, and non-enforcement scope for hardware <code>may-use</code> tuples.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>RFD 2200 shipped the role tuples as data and named enforcement as a separate RFD. Today’s provisioning cycles bit twice on the gap: MPS had to hand-attach an <code>agents-rw</code> policy to HAILO’s entity to unblock a 403 (documented in RFD 2195’s cert-auth-swap gotcha), and the “assist” role change for HAILO landed as a tuple update with no mechanical effect on what capabilities HAILO’s token actually carried. Groups close that gap: a role change is one tuple write and one reconciler run, and the token’s next login reflects the new policies through group membership.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p><code>may-use--&lt;device&gt;</code> tuples stay documentation; Bao does not gate GPU/NPU access. Future compute-lease broker consults them, out of scope here.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>RFD 2200 (tuples as data), RFD 2195 (PKI + cert-auth), RFD 2201 (coordinate-agents ceremony).</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2202-rebac-bao-enforcement/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2202-rebac-bao-enforcement/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2201: The coordinate-agents ceremony</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2201-coordinate-agents-ceremony/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Fold the workspace’s ad-hoc coordination sweep into a named, bounded ceremony that the coordinator role runs when asked. The ceremony has seven steps and each step’s output is a specific artefact — a KV read, a peer message, a PR enqueue, a surface to the operator — with the next step gated on the previous. The steps are enumerated in <code>DETAILS.md</code>; a companion skill <code>coordinate-agents</code> in <code>weftspun/dot-claude</code> is the operational how-to.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>“Coordinate agents” was a sentence the operator said and the coordinator did a pass by memory: check the store, message peers, enqueue clean PRs, surface stuck ones, remember the do-not-touch-peer- branch rule. A pass done by memory shipped good work in one turn and broke a peer’s PR the next, because the safeguards were named retroactively (in RFD 2195 DETAILS, after the mistake) rather than carried in the procedure itself. A named ceremony carries them prospectively.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not a scheduler; not a hook (the prettier-only exception is a shape a hook would reject); not any-agent-runs-this — only the coordinator role from RFD 2200.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>RFD 2195 (Bao PKI + Gotchas), RFD 2200 (roles), <code>agent-sync</code> and <code>coordinate-agents</code> skills in dot-claude.</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2201-coordinate-agents-ceremony/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2201-coordinate-agents-ceremony/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2200: ReBAC agent roles as tuples, not per-agent policy sprawl</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2200-rebac-agent-roles/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Model agent roles as <strong>ReBAC relationship tuples</strong> in a Bao KV mount at <code>relationships/</code>, one tuple per row, key shape <code>&lt;subject&gt;--&lt;verb&gt;--&lt;object&gt;</code>. Twelve tuples live for the current fleet across five verbs (<code>authors</code>, <code>admin</code>, <code>owns</code>, <code>runs-on</code>, <code>hosts</code>). Three named roles fall out — coordinator (MPS), gpu-experimenter (CUDA), edge-qat-specialist (HAILO). See <code>DETAILS.md</code> for the tuple listing and role scopes.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The current <code>agents-rw</code> policy is one shared grant across every peer. Adding a fourth agent that shouldn’t touch GPU code, or a Hailo-specific agent that must not touch the 3090, means either a new per-agent policy (RBAC sprawl) or ad-hoc restrictions in prose. Neither answers the question the shared-$HOME clobber raised: the risk is the <strong>relationship</strong> between two agents, not the identity of either.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Not enforced today. The tuples are data; nothing gates a KV write against them yet. When enforcement lands it goes in a separate RFD. Also not covered: an external ReBAC engine (OpenFGA, SpiceDB) — the tuples-in-KV shim is the minimum that answers the question we have. A production ReBAC engine is a future call.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>RFD 2195 (Bao PKI + agent-provisioning discipline), agent-sync skill in dot-claude.</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2200-rebac-agent-roles/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2200-rebac-agent-roles/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2199: Real 4-bit QAT with Hailo as target hardware</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2199-hailo-real-4bit-qat/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>Parked 2026-09-04 for two reasons the same day. First: Hailo USB firmware stuck in bootloader from a partial update; operator parked rather than requiring admin recovery. Second: the ViT-base BN backbone accuracy delta measured 4.57% under the on-device pipeline — too large to ship — and the vendor fork the measurement ran under cannot be published, so the on-device QAT path at this backbone is not viable. Un-park requires firmware recovery, a resolved BN measurement on a publishable backbone, and a named use case. See <code>[[hailo-ugen300-shelved]]</code> and <code>logbook-rfd-2199-vit-base-bn-pick-provenance.md</code>.</p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Build real 4-bit QAT infrastructure with Hailo as the deployment target. Int4 only. Int8 QAT is not the fallback. If Hailo hardware or the DFC toolchain does not support int4 weights, this RFD terminates with that finding and the operator picks the next target (different NPU vendor, GPU-only int4 QAT via Torchao, or wait for the field).</p>
<p>Model choice deferred to the first spike. <strong>First spike:</strong> verify Hailo int4 support. <strong>Loop:</strong> fake-quant forward, STE backward, single quantized ckpt (Torchao <code>Int4WeightOnlyQuantizer</code> or custom STE). <strong>Model priority:</strong> small mtmd first; workspace target after.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>PR #256 blocks bnb NF4 + post-quantization fine-tuning. PR #257 blocks PTQ. The compliant shape has no framework support for our generative models; the only real-QAT FOSS today is Gemma 4 QAT (language-only). This RFD closes that gap.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>RFD 2186, RFD 2198, PR #256, PR #257, <code>[[hailo-ugen300-shelved]]</code>.</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2199-hailo-real-4bit-qat/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2198: LLaDA-o speed work for the dressing overlay</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2198-lladao-speed-work-for-dressing-overlay/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<p>inference-time dressing overlay generator <code>logbook-lladao-n1-quality-beats-omnigen2.md</code></p>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>LLaDA-o becomes a candidate primary generator for RFD 2186’s dressing overlay, contingent on shipping-viable speed. Current bf16 sharded on two GPUs is a measurement substrate: ~1000 s per edit vs OmniGen2’s ~72 s. Three levers, ordered by session-cost per expected impact:</p>
<ul>
<li><strong>Step-count sweep</strong> across 2/4/8/16/32/50 timesteps. No training, one ladder on shard-90 held-out. Session-scoped.</li>
<li><strong>LCM-style distillation on block diffusion</strong> using upstream <code>train/pretrain_unified_navit.py</code> with our <code>chibifire/editscore-reward-train</code> parquet adapted to <code>UnifiedEditIterableDataset</code> Format B. Multi-day, needs LoRA plus 2-GPU sharded training plus gradient checkpointing.</li>
<li><strong>Pruning or a smaller-variant training.</strong> No smaller LLaDA-o upstream; multi-week; out of scope here.</li>
</ul>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>OmniGen2 baseline on shard-90 held-out: 3.36 mean, 5/20 pairs at 0.00 including subject_remove and material_alter. LLaDA-o at bf16 scored 7.266 on the first of those pairs (n=1). Lumina2-family LoRAs also do not close the gap (<code>logbook-lumina2-distill-n1000-shelved</code>). The obstacle to LLaDA-o is speed, not quality; this RFD scopes closing it.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>RFD 2186, RFD 2183, RFD 1170 (LLaDA-o stays shelved there — different target). Memory <code>[[llada-diffusion-lm-shelved]]</code>. Logbook <code>logbook-lladao-n1-quality-beats-omnigen2.md</code>.</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2198-lladao-speed-work-for-dressing-overlay/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
<item>
  <title>RFD 2197: Restructure maskscore-rung-1-bootstrap for the HF viewer</title>
  <link>https://weftspun.github.io/request-for-discussion/rfd/2197-maskscore-rung-1-bootstrap-hf-restructure/</link>
  <description><![CDATA[ 





<!-- generated by scripts/render_site.py; edit README.md instead -->
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Republish <code>chibifire/maskscore-rung-1-bootstrap</code> as <strong>six configs, one per task-type</strong> (<code>depth</code>, <code>keypoints</code>, <code>mesh</code>, <code>multimodal</code>, <code>pose</code>, <code>speech-audio</code>), each with its base + candidates + scores tables joined into one wide row per example under <code>data/&lt;config&gt;/train-*.parquet</code>. Media satellites (<code>renders/</code>, <code>speech/</code>, <code>poses/</code>, <code>scores/</code>) get referenced by content-hash path or inlined as <code>struct&lt;bytes, path&gt;</code> per RFD 2196 rule 4, depending on file size. See <code>DETAILS.md</code> for the join plan and media strategy.</p>
</section>
<section id="problem" class="level2">
<h2 class="anchored" data-anchor-id="problem">Problem</h2>
<p>The current repository violates RFD 2196 rules 1 and 3: 18 parquets at the repo root (not under <code>data/</code>), organised as six task-types × three satellite tables each. HF viewer preview and viewer both red because the auto-indexer finds no <code>data/train-*.parquet</code> layout it can group into a split. Plus 887 non-parquet files (128 render PNGs, 512 more renders, 150 WAV, 180 VTT, 131 NPZ) whose relationship to the parquet rows the viewer cannot infer.</p>
</section>
<section id="non-goals" class="level2">
<h2 class="anchored" data-anchor-id="non-goals">Non-goals</h2>
<p>Source-of-truth generators (unchanged); rung-2/3/4 datasets; complete schema audit; media-hosting choice if satellites exceed HF’s ceiling.</p>
</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<p>RFD 2196 (HF viewer rules), RFD 2165 (maskscore stub emit), RFD 2166 (maskscore cineform bundle), RFD 1173 (edit-reward corpus).</p>
<p>This RFD was drafted by an AI and read by a human before it shipped.</p>
</section>
<section id="details" class="level2">
<h2 class="anchored" data-anchor-id="details">Details</h2>
<p><a href="../../rfd/2197-maskscore-rung-1-bootstrap-hf-restructure/DETAILS.html">The measurements and the retractions</a></p>


</section>

 ]]></description>
  <guid>https://weftspun.github.io/request-for-discussion/rfd/2197-maskscore-rung-1-bootstrap-hf-restructure/</guid>
  <pubDate>Sat, 05 Sep 2026 22:18:44 GMT</pubDate>
</item>
</channel>
</rss>
