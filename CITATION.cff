cff-version: 1.2.0
message: "If you cite a measurement, a retraction or a constraint from this record, please cite it as below."
title: "logbook: the cross-project engineering record for weftspun"
abstract: >-
  The engineering record behind the weftspun projects: what was measured, what
  it cost, and which roads turned out to be dead ends. Entries record the
  measurement rather than the intention and clip the apparatus, so a result can
  be re-run rather than believed. Retractions stay in place next to what they
  retract, because a reader who knows which roads are dead ends is better off
  than one who only knows the current answer.

  Holds the working agreements every project in the workspace is written
  against (CLAUDE.md), the recurring failure modes and the guards that now
  catch them (PITFALLS.md), the running dated log (todo.md), narrative entries
  on the soft renderer's mis-scaled constants, CineForm in Godot, and the
  one-repo-per-model reranking, and the gates and apparatus that produced the
  numbers.
license:
  - Apache-2.0
  - MIT
authors:
  - family-names: Lee
    given-names: "K. S. Ernest (iFire)"
    email: ernest.lee@chibifire.com
    orcid: "https://orcid.org/0000-0003-4570-1436"
version: 0.1.0
date-released: "2026-08-21"
repository-code: "https://github.com/weftspun/request-for-discussion"
keywords:
  - engineering-logbook
  - reproducibility
  - retraction
  - negative-results
  - measurement
  - working-agreements
  - dataset-provenance
references:
  - type: software
    title: "ONNX: Open Neural Network Exchange"
    abstract: >-
      The operator specification the emitted graphs are written against. The
      ai.onnx domain carries 178 operators at opset 17, of which the target
      accelerator toolchain documents 58. `onnx.defs` is read directly rather
      than copied, so a second list cannot drift from it.
    repository-code: "https://github.com/onnx/onnx"
    license: Apache-2.0
  - type: software
    title: "onnxruntime-extensions"
    abstract: >-
      Where the ONNX ecosystem actually puts text processing: tokenizers and
      string operators as CUSTOM ops under the ai.onnx.contrib domain, not in
      the standard specification. Cited as the reason StringNormalizer is
      refused rather than rewritten -- a custom-op domain has no lowering to
      hardware, so text preprocessing belongs ahead of the graph rather than
      inside it.
    repository-code: "https://github.com/microsoft/onnxruntime-extensions"
    license: MIT
  - type: software
    title: "TorchCodec: PyTorch-native media decoding"
    abstract: >-
      Decodes video, audio and images to tensors on CPU and CUDA. Cited as the
      media counterpart to the same boundary: decoding happens before the
      graph, not in it. It does not handle text, which is why it does not
      answer the string-operator question it was offered for.
    repository-code: "https://github.com/meta-pytorch/torchcodec"
    license: BSD-3-Clause
  - type: software
    title: "mathlib4"
    abstract: >-
      The library the secondary-path proofs are written against. Each exact
      rewrite in hailo_ops.usda that claims an identity is proved in
      RfdetrProofs/Secondary.lean; the six trigonometric rewrites are minimax
      polynomials and are deliberately absent, because a theorem claiming
      equality there would be false.
    repository-code: "https://github.com/leanprover-community/mathlib4"
    license: Apache-2.0
  - type: software
    title: "tinygrad"
    abstract: >-
      Reviewed for better decompositions than the hand-written ones. It
      corroborated Shrink and Hardmax exactly, and its ThresholdedRelu is worse
      for this target because it routes through Where, which the accelerator
      rejects at parse. Its real contribution was negative: the transcendental
      decompositions in codegen/decomp/transcendental.py, derived from SLEEF,
      rest on bitcast, shifts, frexp, ldexp and round-to-int, every one of
      which this target refuses. The polynomial core is plain multiply-add and
      emits; the range reduction that would make it valid outside
      [-pi/2, pi/2] does not.

      MEASURED SINCE, and it strengthens the refusal rather than softening it:
      even where range reduction IS expressible, this target gains nothing from
      it. exp(x) rewritten as exp(x/2)*exp(x/2) leaves the output span unchanged
      at 1.65..79.44 and moves the quantized error only from 0.483141 to
      0.445812. Across four output ranges the error held at 0.5-0.8 percent of
      span regardless of graph shape, so what sets it is bits over span rather
      than how the function is decomposed.

      PARTIALLY RETRACTED, the Where clause above. Where is rejected at parse,
      but it is not unreachable: where(p, a, b) = b + p * (a - b), with the
      predicate built as p = min(max((x - c) * k, -1e-6), 1), compiles and runs
      -- float error 3e-6 at k=100, quantized 0.025573 -- using only Sub, Mul,
      Max, Min and Add. So ThresholdedRelu may be reachable after all, by this
      route rather than through Where.

      The lower clamp must not be literal 0.0. CORRECTED: a first reading of this
      blamed the composition, saying max(x, 0.0) is recognised as ReLU and the
      PAIRED min then fails reaching for clip_min. It is simpler and worse than
      that -- max(x, 0.0) alone is rejected, with nothing after it, and the same
      KeyError: 'clip_min'. The earlier max that compiled had bound 1.0, not 0.0,
      which is what made the composition look guilty. Any exact-zero floor fails;
      -1e-6 compiles and costs 1e-6.

      PIECEWISE LINEAR IS THEREFORE PARTLY OPEN, and it is what NPUs do in
      hardware for exactly these operators. Written as a hinge sum --
      f(x) = f(t0) + s0*(x - t0) + SUM (s_k - s_{k-1}) * max(x - t_k, eps) -- it
      needs only Sub, Max, Mul and Add. Measured: 1, 2 and 3 hinges compile, both
      fanned from one input and chained; 8, 16 and 32 knots do not, failing with
      ValueError: 'base_ew_add6' is not in list. So there is a size limit between
      3 and 8 terms, and it is a graph-shape limit rather than an operator one.

      WITHDRAWN, one commit after it was written: a claim here said the fanned
      hinge form disagrees with the compiler's own float emulation by 0.073750
      per hinge, and offered it as unexplained. It is explained, and it is not
      the compiler. Recomputing the same expression entirely in Nx, with nothing
      compiled, reproduces the error to six decimals -- 1.284628, 0.595394,
      0.245243, 0.099879 across four fits, identical either way. The hinge sum
      was misconstructed: the coefficient at knot k must be slopes[k] minus
      slopes[k-1], and a zip chain had misaligned slopes against knots. The
      compiler was faithfully computing bad arithmetic.

      Corrected, the fit improves an order of magnitude -- 1.284628 to 0.109919
      at three segments -- but four segments still fits WORSE than three, so the
      construction is still wrong and no number from it should be relied on. What
      survives is only the structural result: hinge terms compile at small counts
      and do not at eight or more, which was measured on graph shape rather than
      on any fit being correct.

      The lesson is the reason this paragraph stays rather than being deleted. A
      discrepancy that scales linearly with a parameter looks like a hardware
      story and is much more often an indexing bug, and it was reported as the
      former before the cheap control -- run the same arithmetic with no compiler
      in the path -- had been done.
    repository-code: "https://github.com/tinygrad/tinygrad"
    license: MIT
  - type: software
    title: "SLEEF: SIMD Library for Evaluating Elementary Functions"
    abstract: >-
      The source of the Payne-Hanek and Cody-Waite range reductions that
      tinygrad's transcendental decompositions implement. Cited as the
      provenance of an approach that is correct and unreachable here.
    repository-code: "https://github.com/shibatch/sleef"
    license: BSL-1.0
  - type: article
    title: "cuJSON: A Highly Parallel JSON Parser for GPUs"
    authors:
      - family-names: "Vedadi Gargary"
        given-names: Ashkan
    year: 2025
    collection-title: >-
      Proceedings of the 31st ACM International Conference on Architectural
      Support for Programming Languages and Operating Systems (ASPLOS '26)
    doi: "10.1145/3760250.3762222"
    abstract: >-
      Offered as a route to string operators. It shows structural text parsing
      done data-parallel through prefix sums and precomputed structural arrays,
      and a prefix sum is CumSum, which this table already rewrites as a
      constant lower-triangular MatMul. What it does not remove is the
      obstacle: a parsed structure has a length that depends on its input, and
      data-dependent output shape is the one refusal category a static dataflow
      graph can never express. So the parallelism is available and the shape is
      not, which is why the refusal stands rather than moving.
    repository-code: "https://github.com/AutomataLab/cuJSON"
    license: MIT
  - type: article
    title: "I-BERT: Integer-only BERT Quantization"
    authors:
      - family-names: Kim
        given-names: Sehoon
      - family-names: Gholami
        given-names: Amir
      - family-names: Yao
        given-names: Zhewei
      - family-names: Mahoney
        given-names: "Michael W."
      - family-names: Keutzer
        given-names: Kurt
    year: 2021
    collection-title: >-
      Proceedings of the 38th International Conference on Machine Learning
      (ICML 2021)
    url: "https://arxiv.org/abs/2101.01321"
    abstract: >-
      Offered against two refusals this target makes: Erf, which it rejects
      standing alone and also as the Mul-Erf form the vendor documentation calls
      Gelu, and Exp, which it accepts and then quantizes to 0.483141 of error on
      a 1.65..79.44 output span. I-BERT replaces both with second-order
      polynomials -- i-GELU for erf, i-Softmax for exp -- reporting maximum
      approximation errors of 1.8e-2 and 1.9e-3, and its polynomials are plain
      multiply-add, which is the half of tinygrad's decompositions that DOES
      emit here.

      NOT YET MEASURED against this target, and the gap is worth naming before
      anybody relies on it. i-Softmax still range-reduces with floor and a
      bit-shift, and floor has no lowering. i-GELU needs sign and abs, neither
      of which lowers either -- though abs is max(x, -x), and the sign cancels
      in x * erf(x) because x * sign(x) is abs(x). So the erf half looks
      reachable with operators that already compile and the exp half does not,
      which is the opposite of what the paper's error figures alone suggest.
    license: MIT
  - type: software
    title: "Qwen-Image-Edit-2511-Multiple-Angles-LoRA"
    authors:
      - family-names: Odin
        given-names: Lovis
      - name: "fal"
    url: "https://huggingface.co/fal/Qwen-Image-Edit-2511-Multiple-Angles-LoRA"
    license: Apache-2.0
    abstract: >-
      The source of the camera vocabulary this workspace names views with:
      8 azimuths at 45 degree steps as front, front-right quarter, right side,
      back-right quarter, back, back-left quarter, left side and front-left
      quarter; 4 elevations as low-angle at -30, eye-level at 0, elevated at 30
      and high-angle at 60; and 3 distances as close-up at 0.6, medium at 1.0
      and wide at 1.8. `weft_loop.camera_prompt` converts parameters to that
      prompt and `camera_parameters` converts back, both exactly, with the 96
      cells enumerated in `test_weft_loop.py` rather than sampled.

      WHAT IS TAKEN IS THE PHRASE TABLE AND NOTHING ELSE. Qwen-Image-Edit 2509
      and 2511 are blocklisted here, because 20.4B runs on this desk only
      quantised and quantised it corrupts, and these weights are gated on top of
      that. Neither is loaded. A vocabulary is text and carries no licence into
      a corpus, and naming a view the way the ecosystem already names it is the
      whole benefit.

      Its stated methodology is the 96-pose grid over "3000+ Gaussian Splatting
      renders", trained on fal's own trainer. The page does not name the engine,
      the assets or the image count, so the grid is what is reusable and the
      corpus behind it is not reproducible from what is published.
