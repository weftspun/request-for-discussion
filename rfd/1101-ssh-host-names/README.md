# RFD 1101: Two SSH hosts only, `DGX-Local` and `DGX-Remote`

**Retired:** the original argument lives in git history; consult `git log -- 1101-ssh-host-names/README.md` if a citation leads here.

**State:** abandoned

This RFD was drafted by an AI and read by a human before it shipped.
